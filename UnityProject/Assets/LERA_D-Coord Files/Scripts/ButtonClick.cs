using UnityEngine;
using System.Collections;

public class ButtonClick : MonoBehaviour {

    /*

	public string buttonName;
	private GameObject tagObject;



	private GameObject getParentTag(GameObject inputObject)
	{
		if (inputObject.name.Contains("PointMarker"))
			return inputObject;
		return (getParentTag(inputObject.transform.parent.gameObject));
	}



	public void callButton()
    {
		switch (buttonName)
		{
		case "audio-note":
			tagObject.GetComponent<ButtonActions>().addAudioNote();
			break;
		case "inplace-sketch":
			tagObject.GetComponent<ButtonActions>().addInPlaceSketchObject();
			break;
		case "dimension":
			tagObject.GetComponent<ButtonActions>().addDimensionObject();
                break;
        case "delete":
            tagObject.GetComponent<ButtonActions>().deleteTag();
            break;
		default:
			Debug.Log ("Incorrect button name on " + gameObject.name + " on " + tagObject.name);
			break;
		}
	}



	// Use this for initialization
	void Start () {
		tagObject = getParentTag(gameObject);
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    */
}
